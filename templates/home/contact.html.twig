<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ pageTitle }}</title>
    <link rel="stylesheet" href="{{ asset('tailadmin/style.css') }}">
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <style>
        .menu-item {
            transition: all 0.3s ease;
        }
        .menu-item.active {
            color: rgb(32, 129, 74);
            border-bottom: 2px solid rgb(32, 129, 74);
        }
        .form-control {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 1px solid #d1d5db;
            border-radius: 0.5rem;
            font-size: 1rem;
            transition: all 0.2s;
        }
        .form-control:focus {
            outline: none;
            border-color: rgb(32, 129, 74);
            box-shadow: 0 0 0 3px rgba(32, 129, 74, 0.1);
        }
        .form-label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: #374151;
        }
        .form-error {
            color: #dc2626;
            font-size: 0.875rem;
            margin-top: 0.25rem;
        }
    </style>
</head>
<body class="bg-white">
    <!-- Navigation -->
    <nav class="sticky top-0 z-50 border-b border-gray-200 bg-white/80 backdrop-blur-sm">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex h-16 items-center justify-between">
                <div class="flex items-center gap-3">
                    <a href="{{ path('app_home') }}">
                        <img src="{{ asset('sara.png') }}" alt="SARA" class="h-10 w-auto" />
                    </a>
                    <span class="text-xl font-bold" style="color: rgb(32, 129, 74);">SARA</span>
                </div>
                <div class="flex items-center gap-4">
                    <a href="{{ path('app_home') }}" class="text-sm font-medium text-gray-700 hover:text-brand-600">
                        Accueil
                    </a>
                    <a href="{{ path('app_how_it_works') }}" class="text-sm font-medium text-gray-700 hover:text-brand-600">
                        Comment ça fonctionne ?
                    </a>
                    <a href="{{ path('app_login') }}" class="text-sm font-medium text-gray-700 hover:text-brand-600">
                        Connexion
                    </a>
                    <a href="{{ path('app_login') }}" class="rounded-lg bg-brand-500 px-4 py-2 text-sm font-medium text-white hover:bg-brand-600 transition-colors">
                        Accéder à l'application
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Section: Contact -->
    <section class="py-20 bg-white">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8">
            <div class="mx-auto max-w-3xl">
                <div class="text-center mb-12">
                    <h1 class="text-4xl font-bold tracking-tight text-gray-900 sm:text-5xl mb-6">
                        Contactez-nous
                    </h1>
                    <p class="text-xl text-gray-600">
                        Une question ? Un besoin d'information ? N'hésitez pas à nous écrire, nous vous répondrons dans les plus brefs délais.
                    </p>
                </div>

                <!-- Messages flash -->
                {% for message in app.flashes('success') %}
                    <div class="mb-6 rounded-lg bg-green-50 border border-green-200 p-4">
                        <div class="flex items-center gap-3">
                            <svg class="h-5 w-5 text-green-600" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                            </svg>
                            <p class="text-green-800 font-medium">{{ message }}</p>
                        </div>
                    </div>
                {% endfor %}

                {% for message in app.flashes('error') %}
                    <div class="mb-6 rounded-lg bg-red-50 border border-red-200 p-4">
                        <div class="flex items-center gap-3">
                            <svg class="h-5 w-5 text-red-600" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                            </svg>
                            <p class="text-red-800 font-medium">{{ message }}</p>
                        </div>
                    </div>
                {% endfor %}

                <!-- Formulaire de contact -->
                <div class="bg-white rounded-2xl border border-gray-200 shadow-lg p-8">
                    {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
                    
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <!-- Prénom -->
                        <div>
                            {{ form_label(form.firstName, null, {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.firstName, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.firstName, {'attr': {'class': 'form-error'}}) }}
                        </div>

                        <!-- Nom -->
                        <div>
                            {{ form_label(form.lastName, null, {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.lastName, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.lastName, {'attr': {'class': 'form-error'}}) }}
                        </div>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <!-- Email -->
                        <div>
                            {{ form_label(form.email, null, {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.email, {'attr': {'class': 'form-error'}}) }}
                        </div>

                        <!-- Téléphone -->
                        <div>
                            {{ form_label(form.phone, null, {'label_attr': {'class': 'form-label'}}) }}
                            {{ form_widget(form.phone, {'attr': {'class': 'form-control'}}) }}
                            {{ form_errors(form.phone, {'attr': {'class': 'form-error'}}) }}
                        </div>
                    </div>

                    <!-- Sujet -->
                    <div>
                        {{ form_label(form.subject, null, {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.subject, {'attr': {'class': 'form-control'}}) }}
                        {{ form_errors(form.subject, {'attr': {'class': 'form-error'}}) }}
                    </div>

                    <!-- Message -->
                    <div>
                        {{ form_label(form.message, null, {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.message, {'attr': {'class': 'form-control'}}) }}
                        {{ form_errors(form.message, {'attr': {'class': 'form-error'}}) }}
                    </div>

                    <!-- Bouton d'envoi -->
                    <div class="pt-4">
                        <button 
                            type="submit" 
                            class="w-full rounded-lg bg-brand-500 px-6 py-3 text-base font-semibold text-white shadow-sm hover:bg-brand-600 transition-colors focus:outline-none focus:ring-2 focus:ring-brand-500 focus:ring-offset-2"
                        >
                            Envoyer le message
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>

                <!-- Informations complémentaires -->
                <div class="mt-12 grid grid-cols-1 md:grid-cols-3 gap-6">
                    <div class="text-center p-6 rounded-lg bg-gray-50">
                        <div class="flex justify-center mb-4">
                            <svg class="h-8 w-8 text-brand-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z" />
                            </svg>
                        </div>
                        <h3 class="font-semibold text-gray-900 mb-2">Email</h3>
                        <p class="text-sm text-gray-600">contact@sara.education</p>
                    </div>

                    <div class="text-center p-6 rounded-lg bg-gray-50">
                        <div class="flex justify-center mb-4">
                            <svg class="h-8 w-8 text-brand-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z" />
                            </svg>
                        </div>
                        <h3 class="font-semibold text-gray-900 mb-2">Téléphone</h3>
                        <p class="text-sm text-gray-600">01 23 45 67 89</p>
                    </div>

                    <div class="text-center p-6 rounded-lg bg-gray-50">
                        <div class="flex justify-center mb-4">
                            <svg class="h-8 w-8 text-brand-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                            </svg>
                        </div>
                        <h3 class="font-semibold text-gray-900 mb-2">Réponse</h3>
                        <p class="text-sm text-gray-600">Sous 24-48h</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="py-20 bg-brand-600">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8">
            <div class="mx-auto max-w-2xl text-center">
                <h2 class="text-3xl font-bold tracking-tight text-white sm:text-4xl mb-6">
                    Prêt à commencer ?
                </h2>
                <p class="text-lg leading-8 text-brand-100 mb-8">
                    Rejoignez la communauté SARA et transformez l'accompagnement éducatif de votre enfant.
                </p>
                <div class="flex items-center justify-center gap-x-6">
                    <a href="{{ path('app_login') }}" class="rounded-lg bg-white px-6 py-3 text-base font-semibold text-brand-600 shadow-sm hover:bg-gray-50 transition-colors">
                        Se connecter
                    </a>
                    <a href="{{ path('app_home') }}" class="text-base font-semibold text-white hover:text-brand-100">
                        Retour à l'accueil
                    </a>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="bg-gray-900">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-16">
            <div class="flex flex-col items-center justify-center gap-6">
                <div class="flex items-center gap-3">
                    <img src="{{ asset('sara.png') }}" alt="SARA" class="h-12 w-auto" />
                    <span class="text-2xl font-bold" style="color: rgb(32, 129, 74);">SARA</span>
                </div>
                <p class="text-sm text-gray-400">
                    © 2024 SARA. Plateforme d'accompagnement éducatif.
                </p>
                <div class="flex gap-6 text-sm text-gray-400 flex-wrap justify-center">
                    <a href="{{ path('app_home') }}" class="hover:text-white">Accueil</a>
                    <a href="{{ path('app_how_it_works') }}" class="hover:text-white">Comment ça fonctionne ?</a>
                    <a href="{{ path('app_how_it_works') }}#faq" class="hover:text-white">FAQ</a>
                    <a href="{{ path('app_contact') }}" class="hover:text-white">Contactez-nous</a>
                    <a href="{{ path('app_privacy_policy') }}" class="hover:text-white">Politique de confidentialité</a>
                    <a href="{{ path('app_legal_notice') }}" class="hover:text-white">Mentions légales</a>
                </div>
            </div>
        </div>
    </footer>
</body>
</html>

